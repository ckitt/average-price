spring:
  application:
    name: price-producer
  cloud:
    config:
      uri: http://localhost:8080
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.2